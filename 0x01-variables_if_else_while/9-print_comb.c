#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		if (n != 0)
		{
			putchar(,);
			putchar(32);
		}
		putchar(n);
	}
	putchar(10);
	return (0);
}


