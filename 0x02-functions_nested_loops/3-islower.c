/**
 * _islower - check if a character is lower.
 *
 * @c: integer to be tested.
 * Return: 1 if c is slower, 0 oterwise.
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);


}
